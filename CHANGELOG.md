# Changelog

本项目所有显著变更记录于本文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

### Fixed（fix）

- **Win 更新 lark-cli 报「'C:\Program' 不是内部或外部命令」（npm_failed）**：`C:\Program Files\nodejs\npm.cmd` 含空格，spawn(shell:true) 裸拼可执行路径，cmd.exe 把 `C:\Program` 拆成命令。统一为全部子进程调用（npm 安装/镜像重试/免权限降级、设备授权发起/完成、版本验证、lark-cli 执行）引入 Windows 路径引号包裹。

---

## [0.3.21] - 2026-09-03

### Added（feat）

- **手动删除节点**：总览文档树节点 hover 新增删除按钮（🗑），确认后本地 .md 移入 `.trash-bin/`（镜像相对路径，可手工找回），documents + sheet_sheets 行事务硬删，树即时刷新；文件已被手动删除的残留行直接清除。回收站行（云端删除）拒绝该入口（409，走回收站面板）；行不存在幂等成功。云端仍存在的节点下次检测会列为新增——不勾选同步即不会回来。

### Fixed（fix）

- **手动删除本地文件后的视图残留**：「刷新索引」新增本地缺失行清理（`pruneMissingLocalDocs`）——DB 中 local_rel_path 指向的文件已不存在的活行被硬删，消除视图树残留节点；回收站行与云端身份未落盘的行不受影响，路径越界数据跳过。重建索引 toast 汇报清理数量。

---

## [0.3.20] - 2026-09-03

### Changed（ui）

- **同步进度顶部常驻 + 真实数值 + 当前文档名**：进度条从页面底部右栏迁至同步主区顶部 sticky（滚动中始终可见）；同步提交改为逐文档串行（后端本就按文档独立原子提交，语义等价），进度百分比与「完成/总数」均为真实数值，并实时显示当前正在同步的文档标题与失败计数；全部完成后聚合为单份结果报告，单篇网络层失败不再中断整批。

---

## [0.3.19] - 2026-09-03

### Fixed（fix）

- **lark-cli 安装/更新装错包（根因修复）**：npm registry 上的 `lark-cli` 是无关的 0.1.0 占位包，真正的 CLI 是 `@larksuite/cli`（最新 1.0.93）。此前 `npm install -g lark-cli@latest` 一直在安装假包——「可更新至 0.1.0」与「无法更新到最新」的真正根因（部分机器上假包被 PATH 遮蔽而显示旧版本号，Win 上则直接失败）。安装/镜像重试/免权限降级三处与最新版查询全部改用 `@larksuite/cli`；真实验证 1.0.89 → 1.0.93 更新成功。

---

## [0.3.18] - 2026-09-03

### Added（feat）

- **lark-cli 版本真实对比着色**：状态卡片新增 registry 最新版查询（npm view，15s 超时软失败）——本机已最新→绿色「已最新」；落后→琥珀色「可更新至 X.Y.Z」；查询失败→中性色仅显示当前版本。消除旧版无条件绿色导致的「是否最新」误导（如 Win 1.0.53 / mac 1.0.89 均非最新 1.0.93）。

---

## [0.3.17] - 2026-09-03

### Fixed（fix）

- **同步页「立即检测」按钮失效**：按钮此前仅重读 cached 存量 diff（cached=1 不触发云遍历），从不执行真实检测——轮询不频繁的环境（如 Win）感知为点击无效。现改为先发真实检测（多根走 changes-all，含媒体完整性核对 full 作用域；单根走单根检测），完成后再读持久化结果刷新列表。

---

## [0.3.16] - 2026-09-03

### Added（ui）

- **专属应用图标**：替换通用 Electron 图标为纸墨「飞」印章设计——宣纸纹理白底、朱红方印内白色楷书简体「飞」字、下方青玉色双向水墨弧线构成的同步意象；mac 使用全尺寸 icns，Windows 由 1024 源图自动转换多尺寸 ico，与应用内印章 logo 一脉相承。

---

## [0.3.15] - 2026-09-03

### Changed（ui）

- **认证卡片「lark-cli 版本」徽标改绿色**：版本信息属状态展示而非警示，由印章红（text-seal）改为成功绿（text-jade），与「已就绪」语义一致。

---

## [0.3.14] - 2026-09-03

### Fixed（fix）

- **更新 lark-cli 报 spawn E2BIG（npm_failed）**：重度用户机器上 fnm multishells 累积可达 15 万个目录，全量拼入子进程 PATH 直接撑爆环境变量块。三层修复：①候选目录扫描限量（multishell 取 mtime 最近 8 个、已装版本取最新 5 个）；②npm 执行环境改为最小注入（仅 npm 所在目录 + fnm current，候选集仅供发现阶段）；③PATH 总长 28,000 字符守卫，超限退化为最小注入，绝不让环境块爆掉。

---

## [0.3.13] - 2026-09-03

### Fixed（fix）

- **「更新 lark-cli」按钮在认证未就绪时消失（Win 用户反馈）**：按钮原仅在认证就绪态渲染，而认证缺权限时恰是用户最需要更新 CLI 的时刻。现改为「lark-cli 已安装即显示」，与认证状态解耦。

---

## [0.3.12] - 2026-09-03

### Fixed（fix）

- **认证后误报「缺少权限：docs:document:read」**：飞书新版 scope 体系为 docx 读取引入新名 docs:document:read（与旧名 docx:document:readonly 并存，lark-cli ≥1.0.8x docs 命令族依赖新名）。修复三件套：①默认/存量配置请求集追加新名；②权限比对引入新旧名等价组，旧授权只持旧名时不再误报、无需重新授权；③缺权限文案改为引导应用内「开始飞书认证」一键补齐。

---

## [0.3.11] - 2026-09-03

### Fixed（fix）

- **零散归档文档补全覆盖**：custom folder（_custom/）媒体核对候选集从仅 sheet 扩展为全部 synced 归档文档——零散 docx/slides 的白板与媒体残留占位标签现在也参与本地扫描（local-only 即生效，零 API）；sheet 云端清单核对仍限「立即检测」full 作用域。检测→落库 pending_modified→变更列表→归档同步的完整闭环对全类型零散文档生效。

---

## [0.3.10] - 2026-09-03

### Fixed（fix）

- **媒体补齐项在变更列表不可见（stored-diff 断链）**：media-gap 检测项此前仅存在于检测响应内存中，未落库——变更列表从 SQLite pending 状态重建，导致用户无法勾选同步补齐。现在检测到媒体缺口时将文档置为 pending_modified（不动 synced 基线），同步完成后自动收敛回 synced；未同步时下一次轮询会洗回 synced，可再次「立即检测」重检（sheet 云端清单核对仅主动检测触发的既定语义）。

---

## [0.3.9] - 2026-09-03

### 概要
v0.3.9：媒体完整性核对（media-gap）落地与分层限流防护，修复 lark-cli 1.0.89 ndjson 契约断裂导致的检测㐫疾，历史欠账图片一次性补齐。

### Added（feat）

- **媒体完整性核对（media-gap 检测通道）**：变更检测不再纯依赖 obj_edit_time——本地扫描 docx 白板/媒体残留占位标签（零 API），主动检测时核对 sheet 云端浮动图片清单与本地引用，历史欠账文档重新进入同步管线；前端变更列表新增「图片补齐」徽标。
- **分层限流防护**：轮询默认 local-only（零云端调用）；「立即检测」显式 full 作用域；workbook-info 结果 5 分钟缓存，消除背靠背核对触发飞书账号级 99991400 限流的问题。
- **媒体补齐运维脚本**：server/scripts/verify-media-gap.ts（只读核查）与 repair-media-gaps.ts（dry-run/apply 补齐）。

### Fixed（fix）

- **lark-cli ≥1.0.89 ndjson 裸实体流解析缺陷（真实事故）**：wiki +node-list 的裸节点流被页合并器坍缩，BFS 返回空树却报 complete=true，导致 146 个 synced 文档被误标删除候选（missing_candidate）。解析层新增裸实体流聚合（data.records），listWikiNodes 双代际输出兼容；missing_candidate 状态机自动恢复验证通过。

### Optimized（ui）

- **版本号按钮合并至左上角**：移除总览页状态栏冗余版本胶囊，应用标题下方常驻紧凑版本按钮（新版本提醒/下载进度/一键检查更新），顶部导航更简洁。

---

## [0.3.8] - 2026-09-03

### 概要
v0.3.8：总览页版本更新快捷入口、构建产物体积深度优化（104MB→76MB）、lark-cli 安装更新异常修复与表格（Sheet）图片完整同步保全。

### Added（feat）

- **总览页版本更新快捷入口与版本号提示**：
  - 顶部全局状态栏（GlobalStatusBar）挂载常驻版本快捷入口，动态展示当前版本、更新状态与下载进度，支持一键快速检查更新与一键直达设置；
  - 总览页顶部新增版本更新横幅（VersionUpdateAlert），根据版本号智能对比当前版本与目标版本，识别重大/功能/补丁更新级别并展示发行说明，支持一键下载、前往更新和稍后提醒；
  - 重构全局更新状态机（useDesktopUpdate）为模块级单例，保证总览页、顶部栏、设置区卡片实时同步联动；
  - 导航系统接入 focusNonce 机制，确保从总览页重复点击时均能 100% 切换并聚焦到「设置 · 关于与更新」卡片。
- **飞书表格（Sheet）图片完整同步与保全**：
  - 新增 `sheet-media` 模块，自动探测子表浮动图片元数据（`sheets +float-image-list`）；
  - 遵循直链 fetch → 官方媒体导出 → 预览流兜底的三层安全降级下载通道，下载至 `images/`；
  - 引号感知 CSV 智能回填：根据单元格坐标（如 A7）将图片引用精准注入 CSV 单元格，使表格重构后单元格内自然包含图片；
  - 子表 Markdown 尾部追加「子表图片资源」大图展示附录，确保复杂架构图与流程图清晰可见；
  - 接入 DocumentIR 与 atomic-commit 原子提交流水线，受事务与回滚机制保护，并在 SyncEngine 主链路和自定义归档双链路闭环生效。

### Fixed（fix）

- **lark-cli 更新与安装失败（npm_failed）修复**：
  - 为子进程执行注入完备的 Node 运行时环境变量，全量支持 fnm, nvm, volta, pnpm, asdf, Homebrew 等现代版本管理器候选路径，杜绝 Finder 启动时精简 PATH 导致找不到 `node`；
  - 增加全局安装权限不足（EACCES/EPERM）时自动回退至用户免 root 目录 `~/.npm-global`；
  - 增加网络超时自动切换国内官方镜像重试与前端错误日志完整展开。

### Optimized（perf & build）

- **桌面构建产物体积深度瘦身（-28MB / -27%）**：
  - 优化根目录依赖结构，将已在构建期打包的纯前端库（React、Lucide 等）移至 devDependencies，消除 `app.asar` 冗余依赖（从 20MB 降至 1.4MB）；
  - 消除根目录与 server 依赖双重打包及 native 模块重复 unpack；
  - 清理 `better-sqlite3` 中 24MB 的 C++ 源码、静态库与编译目标中间文件；
  - 排除 50+ 个 Chromium 未使用国际化语言包并启用 maximum 压缩，DMG 安装包从 104MB 骤降至 76MB。

---

## [0.3.7] - 2026-09-01

### 概要
v0.3.7：跨视图状态实时联动与应用内更新完整闭环——同步/检测完成后各视图自动刷新，应用内更新正式接入 GitHub Releases。

### Added（feat）

- **应用内更新接入 GitHub Releases**：运行时更新源从占位地址切换为 `releases/latest/download/`（与构建侧 publish 配置同源，支持 `DESKTOP_UPDATE_FEED_URL` 覆盖），打通 electron-updater 检查→下载→安装全链路。
- **启动静默检查与新版本徽标**：应用启动 15 秒后自动静默检查更新，发现新版本时 TopBar 亮起可点击「新版本」徽标，直达设置→关于与更新。
- **更新卡片重构**：对齐主进程更新契约（phase/latestVersion），基于事件流实时渲染下载进度（百分比/体积/速率）、发行说明与「打开下载页」兜底（macOS adhoc 构建不支持应用内安装时的主路径）。
- **平台能力透出**：新增 `desktop:get-platform-capabilities` IPC，渲染层动态识别打包状态、平台更新能力与发布页地址。

### Fixed（fix）

- **同步完成后总览待同步数量不实时更新**：新增跨视图 diff 变更事件总线，检测/同步/回收站等写路径完成后，待同步计数、变更列表与最近变更全部自动重拉。
- **总览侧操作后同步页变更列表滞后**：变更列表面板订阅全局事件，切回同步主区时自动重读（兼容服务端定时检测造成的无事件过时），不再必须手动点「立即检测」。
- **检查更新 404 误报「已是最新」**：更新源 404 现在如实报错并给出修复指引，不再静默谎报。
- **「打开数据目录」误报失败**：前端读取的 `success` 字段在主进程契约中从未存在（实为 `ok`），对齐后成功不再弹「打开目录失败」警告。

### Fixed（ci）

- **mac 双架构自动更新馈送竞态**：arm64/x64 两个构建各自生成同名 `latest-mac.yml` 竞速上传，后到者被「存在即跳过」，导致已发布馈送只含单一架构条目、另一架构应用自动更新会拿到错误架构安装包。现在 arm64 构建改用 `latest-arm64` 通道（产出并上传 `latest-arm64-mac.yml`），x64 保持 `latest-mac.yml`，互不冲突。

---

## [0.3.6] - 2026-08-25

### 概要
v0.3.6：新增节点检测修复——「立即检测」现能发现知识库子树中新建的云文档，不再只追踪已建立索引的节点。

### Fixed（detect）

- **fast 模式新增节点结构性漏检（fast-added-fix）**：fast 检测原先只批量比对已映射文档的 Drive 元数据、不遍历 wiki 拓扑，添加连接建立基线后云端新出现的节点永远不会进入待同步列表。现在 fast 路径在元数据比对后追加增量拓扑发现：每父节点一次 `wiki +node-list` 找出本地无行的新节点，各一次 `wiki +node-get` 解析编辑时间，产出 `pending_added` 待同步项；无新节点时零额外 node-get，成本可控。
- **直挂根节点下新文档的父链投影**：`projectParentChains` 补 `parentToken === rootToken` 特判，fast 路径不播种 root observation 时新节点路径规划不再 fail-closed。
- **发现过程的失败边界**：任一层 node-list 失败即跳过本次新增发现（不影响既有元数据比对）；删除/移动推断仍保持为 full 模式职责。

---

## [0.3.5] - 2026-08-24

### 概要
v0.3.5：飞书认证引导闭环——支持应用内检测、一键安装/升级 `lark-cli` 并通过 Device Flow 登录授权，极大降低首次配置与使用门槛。

### Added（feat）

- **应用内 `lark-cli` 状态检测与一键安装/更新**：新增 `LarkCliManager` 模块，支持检测 Node/npm 环境、检查 `lark-cli` 是否安装与最新版本，并在设置页提供一键安装与更新能力（安装过程独立执行，不阻塞变更检测队列）。
- **Device Flow 浏览器免密授权引导**：新增设备流认证接口（发起与完成），未登录时点击即可自动拉起系统默认浏览器进入飞书授权页，用户授权后自动完成状态刷新，无需手动打开终端输入命令。
- **安全外部链接跳转**：Electron 新增 `desktop:open-external` IPC 通信（带 http/https 协议白名单），安全调起外部浏览器。
- **AuthSettingsCard 引导式体验**：未安装时展示一键安装按钮与环境状态（缺失 Node 时引导下载），未登录时清晰展示认证码与授权链接，提供取消与重试机制。

---

## [0.3.4] - 2026-08-24

### 概要
v0.3.4：发布链路跨平台补全——GitHub Actions 新增 Windows x64 构建，Release 产物覆盖 macOS（arm64/x64）+ Windows（x64）三平台。

### Added（ci）

- **Windows x64 官方构建**：Release 工作流新增 `build-win` 任务（windows-latest / NSIS），产出 `FeishuSync-Setup-<版本>-x64.exe` 安装包与 `latest.yml` 更新馈送，Windows 端接入 electron-updater 自动更新链路。
- **既有 Release 补传能力**：`workflow_dispatch` 手动触发支持指定 `tag`，向已发布版本补传缺失产物（不新建 release）。

### Fixed（ci）

- **发布资产上传竞态**：多矩阵任务并发上传同名资产（如 `latest-mac.yml`）时 `--clobber` 先删后传存在竞态窗口，实测曾导致 422 与资产短暂丢失；改为「存在即跳过 + 并发兜底重查」，彻底消除覆盖与丢失风险。

---

## [0.3.3] - 2026-08-24

### 概要
v0.3.3：自定义归档增强——全量重建自动纳管 `_custom` 归档子目录、未归属文档最长前缀路径回填绑定与飞书结构树归档隔离。

### Added / Improved（feat）

- **全量重建自动纳管自定义归档目录**：`IndexScanner` 扫描知识库本地文件时，自动识别 `_custom/` 下包含 Markdown 的一级子目录并注册为自定义归档文件夹，无需手动逐个创建。
- **最长路径前缀批量回填绑定**：`LocalMapStore` 新增 `backfillCustomFolders`，对未关联归档 ID 的已有本地文档，按相对路径最长前缀精准回填 `custom_folder_id`。
- **飞书结构树归档文档视图隔离**：`MappingService` 飞书结构树投影自动过滤已关联归档的文档记录，确保归档文档只在对应的自定义归档分区展示，避免双重展示。

---

## [0.3.2] - 2026-08-24

### 概要
v0.3.2：快捷添加双模式支持与一键跳转定位、书签单组独立过滤与一级视图收敛、设置页归档管理子页与单篇移出归档能力、三栏工作台比例联动与预览渲染优化。

### Added（feat）

- **快捷添加双模式与跳转定位**：快捷添加弹窗支持「零散云文档」（单篇归档）与「同步根目录」双模式；录入成功或检测到文档已存在时，提供一键「在文档树中定位」跳转，并自动触发树展开与高亮选中。
- **书签单组过滤与视图收敛**：节点树根目录书签栏支持单组独立过滤与快速收敛至对应根目录或归档视图；接入外部 `focusRequest` 实现平滑滚动与闪烁定位。
- **归档管理子页与文档移出**：设置页新增「自定义归档管理」子页，支持查看归档详情、文档列表、修改名称与颜色；后端新增单篇文档从归档移出接口（`DELETE /api/custom-folders/:id/documents/:token`）。
- **开发与调试支持**：支持 `FEISHU_SYNC_DEV_TOKEN` 与环境变量端口注入，开发模式支持动态代理鉴权与本地 CORS 放行。

### Changed / Improved

- **工作台布局与空间优化**：Dashboard 右侧详情栏默认收起，展开时智能等比压缩左栏宽度（保底 220px），最大化保障主预览区域阅读体验。
- **Markdown 渲染增强**：预览过滤内部 HTML 注释与 `<callout>` 等同步标记标签；优化单破折号表格语法容错与特殊符号反斜杠转义。

---

## [0.3.1] - 2026-08-24

### 概要
v0.3.1：节点树新增根目录锚点条——分组头不再被展开内容挤掉。

### Added（feat）

- **根目录锚点条**：搜索栏下方新增常驻书签栏，按 watchedRoot 分组 + 未分类 + 自定义归档列出锚点（图标 + 名称 + 节点数）；树滚动时书签栏固定可见，点击书签展开已收起的分组并平滑滚动到对应分组头；已收起分组的书签以虚线边框区分。

---

## [0.3.0] - 2026-08-24

### 概要
v0.3.0：总览/同步/设置三大主区布局系统性重构，新增文档内容预览（MD/CSV/图片）；移除 claude-cli 与 opencode 本地无头整理通道（收敛为 direct 单通道）。

### Added（feat）

- **总览区三栏工作台**：主内容区全宽化，左节点树 + 中文档预览（主区域）+ 右详情侧栏；左栏可拖拽调宽（240–560px，双击复位，宽度持久化），右栏可点击收起为竖条。
- **文档预览面板**：点击节点即以 Markdown / CSV 双页签预览本地内容——自研零依赖 MD 渲染器（标题/代码块/GFM 表格含列对齐与转义竖线/嵌套列表/引用/**图片**：`GET /api/mapping/media` 鉴权读取 kbRoot 内图片，blob 渲染，含 `<synced-source>`/`<whiteboard>` 等同步产物标签适配）；CSV 表格支持多子表切换、粘性表头、行号、200 行截断；MD 支持渲染/源码切换；面板含「在文件夹中打开」按钮。
- **键盘导航**：↑/↓ 在节点树可见行中切换选中并联动预览（输入框内不劫持，隐藏主区不响应）。
- **同步区两栏布局**：左变更列表 + 右同步操作侧栏（340px，xl 起 sticky）；「批量同步」接入真实确认+原子写入流程（原为无意义 toast）；「批量跳过」改名「清空选择」。
- **主区常驻挂载**：总览/同步/设置切换仅隐藏不卸载，同步进度、结果报告、勾选项、树选中态在往返切换后原样保留。
- **同步根目录配置改为子页侧边栏模式**：左根目录列表（状态点+名称，动态增删并立即选中）+ 右选中项编辑器，替代卡片平铺。

### Removed（refactor）

- **claude-cli 与 opencode 本地无头整理通道整体移除**：含两个 CLI 通道模块与发现服务、`/api/claude/*`、`/api/opencode/*` 路由、设置页通道切换/安装卡片。文档整理收敛为 direct（OpenAI 兼容端点）单通道；旧配置 `primaryChannel` 读取时自动归一为 `'direct'`，整理失败仍由确定性重建结果兜底。

### Fixed（fix）

- 树搜索框与过滤器此前只自动展开祖先、不过滤渲染；现按「匹配节点 + 祖先链」正确过滤并显示匹配数。
- `ChannelTestRequest.llm` 类型补 `timeoutMs`（后端本就消费该字段）。

---

## [0.2.9] - 2026-08-24

### 概要
v0.2.9：修复打包应用内嵌 server 强占 3001 端口导致启动报错（EADDRINUSE）的 P0 bug。

### Fixed（fix）

- **内嵌 server 端口回退错误**：`startServer` 用 `options.port || DEFAULT_PORT`，Electron 传入的 `port: 0`（系统自动分配）被 `||` 当作 falsy 回退到 3001——打包应用始终强占 3001，与本机其他服务（开发服务器等）冲突时内嵌 server 启动失败、应用报错。改为 `??` 仅在未传端口时回退，Electron 模式恢复随机端口。

---

## [0.2.8] - 2026-08-24

### 概要
v0.2.8：总览区布局重构——三栏全宽工作台 + 文档内容预览（MD/CSV），并修复树搜索过滤失效。

### Added（feat）

- **总览区三栏布局**：主内容区取消 max-w 居中限制（消除宽屏空白），改为左节点树（300–320px）+ 中文档预览（主区域）+ 右详情侧栏（详情/最近变更/孤儿提醒），三栏等高独立滚动。
- **文档预览面板**：点击节点即以 Markdown / CSV 双页签预览本地内容；MD 支持渲染/源码切换，sheet 多子表切换，含骨架屏与分级空态；新增 `GET /api/mapping/content/:objToken`（knowledgeBaseRoot 内安全只读，512KB 截断）。
- **预览面板「在文件夹中打开」按钮** 与 **↑/↓ 键盘切换节点**（联动预览并滚入可视区）。
- 视觉与交互动效：树选中态强化、导航/页签下划线 scaleX 过渡、切换淡入。

### Fixed（fix）

- 树搜索框与过滤器此前只自动展开祖先、不过滤渲染；现按「匹配节点 + 祖先链」正确过滤并显示匹配数。
- `ChannelTestRequest.llm` 类型补 `timeoutMs`；根 tsconfig 排除前端测试文件。

---

## [0.2.7] - 2026-08-19

### 概要
v0.2.7：优化退出协调机制与自定义文档同步稳定性，提升打包与运行可靠性。

### Added（feat）

- **退出协调器（Quit Coordinator）优化**：增强主进程退出与后台轮询协调，提升退出与清理稳定性。
- **自定义归档与同步强化**：进一步健全自定义文件夹文档的变更检测与同步写回链路。

---

## [0.2.6] - 2026-08-12

### 概要
v0.2.6：自定义归档文档纳入同步状态跟踪（云端变更可检测、可同步更新本地）。

### Added（feat）

- **归档文档同步状态跟踪**：快捷添加的自定义归档文档（_custom/）不再只是添加时快照——云端内容变更可通过检测识别（detectCustomFolderChanges，接入检测与轮询）、出现在变更列表（diff 并入 modified）、并可在同步区勾选同步更新本地文件（写回 _custom/，保留归档归属）。
- **纯云文档链接归档修复**：非知识库 /docx/ 链接的 131005 回退在真实 lark-cli 子进程链路可达（错误 code 从 stderr JSON 提取透传），零散文档归档主路径打通。

### Fixed（fix）

- 多 watchedRoot 下归档文档在变更列表/计数中重复 N 倍（前端按 objToken 去重）。

---

## [0.2.5] - 2026-08-12

### 概要
v0.2.5：快捷添加飞书云链接 + 自定义文件夹归档；树层级视觉修正。

### Added（feat）

- **快捷添加云文档**：纵览页左侧树顶部新增「快捷添加云文档」按钮，粘贴飞书链接（知识库内或任意零散 docx）即可添加；支持非知识库纯云文档（131005 回退解析）。
- **自定义文件夹归档**：新建/改名/删除自定义文件夹，将零散云文档归档到指定文件夹（本地落盘 _custom/<文件夹>/，含图片附件）；树底部「自定义归档」分组展示归档文档，可收起/展开，点击联动详情卡；孤儿扫描与对账逻辑已排除归档文件。
- **树层级视觉**：顶层（watchedRoot 分组）文字/图标大于其他层级，深层节点不再与顶层同尺寸；watchedRoot 分组支持点击收起。

### Fixed（fix）

- 归档写入安全链：媒体下载参数顺序、同标题冲突不覆盖、DB 失败快照回滚、非 wiki 回退 host 白名单与 131005 限定、结构树成员拒绝归档、并发归档串行化、对账入口透传归档路径、错误分类修正。

---

## [0.2.4] - 2026-08-12

### 概要
v0.2.4：飞书视图根目录（watchedRoot 分组）支持收起。

### Added（feat）

- **根目录收起**：飞书视图顶层按 watchedRoot 分组的标题行可点击展开/收起（含箭头指示、数量徽标、键盘可达），未分类分组同样支持；默认保持展开。

---

## [0.2.3] - 2026-08-12

### 概要
v0.2.3：修复页面版本信息显示停留在 0.2.0 的问题（硬编码版本号改为构建时注入）。

### Fixed（fix）

- **版本号动态化**：顶部栏与「关于与更新」卡片不再硬编码 v0.2.0，改为 vite 构建时注入 package.json version（桌面端优先显示 Electron 真实版本 app.getVersion），版本信息与实际安装包一致。

---

## [0.2.2] - 2026-08-12

### 概要
v0.2.2：修复切换主区后同步进度丢失、飞书侧待处理面板默认收起。

### Fixed（fix）

- **同步进度跨主区保留**：同步状态（syncing/结果/总数）从 SyncView 局部 state 提升为全局 SyncProvider（Context），切换主区不再丢失，后台同步执行期间切回进度卡照常显示，同步结果列表也保留。
- **飞书侧待处理面板默认收起**：改为默认收起、点击标题行展开/收起（含数量徽标与键盘可达性），减少主界面干扰。

---

## [0.2.1] - 2026-08-12

### 概要
v0.2.1：修复总览区飞书/本地树视图布局显示面积过小、内容显示不全的问题。

### Fixed（fix）

- **树视图布局**：总览区左侧树列 280px 加宽至 340px，总览主区放宽至 1440px（同步/设置区保持 1280px 不受影响）。
- **树行内容完整展示**：飞书/本地树缩进 10px/级 收紧至 8px/级（上限 48→40px），把空间让给标题；飞书树标题增加悬停显示全名（title），深层节点不再无提示截断。
- **lark-cli 认证**：修复 user 身份 refresh token 过期导致认证身份回退为 bot 的问题（需重新执行 `lark-cli auth login` 授权，非代码改动）。

---

## [0.2.0] - 2026-06-18

### 概要
v0.2.0：增改删三态识别 + 节点树可视化 + LLM 双通道（bigmodel）+ 3 主区中国风精简 UI + B1-B8 清偿。

### Added（feat）

- **变更检测三态识别**：区分 `added` / `modified` / `deleted` 三态，闭环 B1 / B7 / B8，支撑真实增量同步与孤儿文件识别。
- **节点树视图**：左侧节点树（含同级拖拽 reorder、业务标记独立标签），对接 mapping/tree 与 mapping/reorder API。
- **DiffReport API**：新增 `/api/mapping/diff` 端点，输出云端-本地差异结构化报告。
- **`_index.json` 快照**：索引扫描产出快照，加速 mapping/index 视图与一致性校验。
- **sheet 子表映射**：飞书电子表格子表维度的映射存储与同步路径。
- **软删除回收站**：删除项进入回收站（trash）表，可恢复；新增 `/api/trash` 端点。
- **LLM 双通道**：
  - `bigmodel`（GLM-5.2 / GLM-4-Flash，OpenAI 兼容协议）
  - `claude-cli`（主通道）+ `direct` 降级通道
  - 两通道共用一份配置（apiKey / baseURL / model），新增 `/api/llm/test-channel` 端点（响应不含 apiKey 字段）。
- **3 主区中国风精简 UI**：壹同步 / 贰浏览 / 叁设置三主区 IA，印章风格 logo 与水墨基调。
- **ErrorBoundary + Toast**：全局错误边界与统一 Toast 反馈，覆盖 loading / success / warning / error 四态。
- **CSV 表格重构**：五类块（标题/段落/列表/表格/CSV）的渲染统一与重构。
- **索引重建端点**：`POST /api/index/rebuild` 调 `IndexScanner.scanKnowledgeBase` 重建 documents 并刷新 `_index.json`（P5 新增）。

### Changed（refactor）

- 服务端引入 `content-backend` 抽象层与多渠道支持，统一 LLM 调用入口与配置迁移。
- 服务端入口选项重构：`corsDevMode` 显式独立（P5），从单一 `desktopMode` 多义信号中分离。

### Fixed（fix）

- **B4**：前端检测链路接入 config，配置变更实时生效。
- **B5**：`IndexScanner` 三格式头兼容（obj_token / parent_node_token / 双 token 头）。
- **B6**：LLM 失败时使用 `reconstructed` 兜底，避免阻塞同步流程。
- **CORS dev 模式**：standalone 入口 `corsDevMode=true` 放行 vite 5173 preflight；生产 Electron 保持严格白名单（`app://feishu-sync.local`）。
- **P0-bug-2 / 索引重建**：rebuild 端点上线后，DB 中 `empty_title` 由 16 → 0，全部 documents `status=synced`。
- **P1-bug-1 / AppUpdateCard dev 崩溃**：新增 `isDesktopUpdateAvailable()` 函数式守卫，覆盖 5 处 `window.desktop.update.*` 访问点；dev:all 模式渲染占位卡片而非触发 ErrorBoundary。

### Known Limitations / Deferred

- **KB 命名规范（NNN- 前缀）**：Q5 推翻了"强制 NNN- 前缀"的前提（与既有约定冲突），将下轮迭代重新定义。
- **同步块下钻**：Q3 已确认飞书当前 API 无同步块下钻语义，不做处理。
- **claude-cli 通道连通测试**：冷启动较慢（进程启动开销），首次 test-channel 可能超时，建议预热或调高超时上限。
- **P2-bug-1**：`top_level_dirs` 仅识别含 `obj_token` 头的 2 个顶层目录（非完整 4 类），为 KB 命名规范未完成的下游表现，非应用 bug。

### Tests

- 服务端单元测试：96 → 105（新增 CORS 中间件 5 用例 + rebuild 路由 4 用例）。
- tsc / vite build / vitest 全部 PASS。
- P5 端到端 + 重测全部 PASS（CORS preflight / rebuild empty_title 16→0 / AppUpdateCard 浏览器实测）。

### 审核记录

- 谛听（diting）P5-fix 审核：CONDITIONAL_PASS，无 Critical / Major，5 项 Minor 可后续处理。
- I1 红线（`lark-cli-client.ts` 零修改）自始至终保持。

---

## [0.1.0] - 初始版本

- 项目骨架（Electron + Vite + React + Hono + better-sqlite3）。
- 飞书文档拉取与本地镜像基础能力。
- 单一 LLM 通道、基础配置与同步流程。
